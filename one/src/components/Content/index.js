import Cards from "./Cards";
import HeroText from "./HeroText";

const Content = () => {
    return ( 
        <section class="flex flex-col col-auto ">
            <HeroText />
            <Cards />
        </section>
     );
}
 
export default Content;